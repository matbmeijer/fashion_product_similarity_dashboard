package <- "dplyr"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "stringr"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "glue"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "tidyr"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}


package <- "FNN"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "DT"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "yaml"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "loggit"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "rvest"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "binman"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "RSelenium"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "keras"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "rmarkdown"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

package <- "flexdashboard"
if(!require(package, character.only=TRUE)){
  install.packages(package)
  require("package")
  message("%s package version is %s.",package, packageVersion(package))
}

